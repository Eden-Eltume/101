numbers = [1, 2, 3, 4, 5]

numbers.delete_at(1)
# It destructively deletes the element at index 1 and returns the element

numbers.delete(1)
# It destructively deletes the element 1 wherever it is in the numbers array and returns the element.
