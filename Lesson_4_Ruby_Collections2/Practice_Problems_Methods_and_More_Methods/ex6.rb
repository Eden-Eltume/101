# The return value of it is 11. 
# The reason is because pop destructively  remove the last element form the array.
# It returns that same element on which array#size is called on.
# The size method returns the integer that corresponds to the length of the string.